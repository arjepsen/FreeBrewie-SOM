#include "App_orchestrator.h"

/*
 * Intentionally empty for now.
 *
 * This file remains in the source tree so the future routing module has an obvious home,
 * but it should not become a fake wrapper around unrelated state. The first real functions
 * added here should route app-level user requests through fault, startup, machine-state, or
 * workflow rules.
 */
