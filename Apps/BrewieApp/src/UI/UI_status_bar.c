#include "UI_status_bar.h"

/*
 * Reserved for a shared status/header component.
 *
 * If multiple screens need the same title, link, time, or alert strip, it should be built
 * here so every screen does not drift visually.
 */
