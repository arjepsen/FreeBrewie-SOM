#include "UI_dialog.h"

/*
 * Reserved for modal dialogs.
 *
 * Shared dialogs should live here instead of being recreated inside every screen. That keeps
 * confirmation, warning, and error behavior visually consistent.
 */
