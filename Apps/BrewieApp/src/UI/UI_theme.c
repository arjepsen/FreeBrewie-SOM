#include "UI_theme.h"

/****************************************************************************************
 * @brief Initialize shared UI theme resources.
 *
 * Most current styling is still screen-local while the old Brewie screens are being
 * recreated. Keep this hook so shared styles can move here gradually without changing the
 * top-level UI initialization flow.
 ****************************************************************************************/
void ui_theme_init()
{
}
